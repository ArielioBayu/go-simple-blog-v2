services:
  db:
    image: mysql:latest
    container_name: simple-blog-v2
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=secret
      - MYSQL_DATABASE=db-simple-blog
    volumes:
      - "./db:/var/lib/mysql"